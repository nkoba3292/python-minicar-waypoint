# 74HC590 ピン配置検証

## 現在の診断結果

### GPIO 読み取り値（Sample 2-10）
```
BCM 13 (D0) = 1  ← 期待: QA (pin 15)
BCM 21 (D1) = 0  ← 期待: QB (pin 1)  ★常に0で異常★
BCM 25 (D2) = 1  ← 期待: QC (pin 2)
BCM 17 (CCLR) = 1
BCM 24 (RCLK) = 1
```

D0,D1,D2 = 101 = 5（固定）

### 東芝 TC74HC590AP ピン配置（16ピンDIP）

```
        ┌─────┴─────┐
   QB  1│           │16  VCC
   QC  2│           │15  QA
   QD  3│           │14  SI (Serial In)
   QE  4│           │13  RCLK (Register Clock)
   QF  5│  74HC590  │12  G' (Output Enable, active LOW)
   QG  6│           │11  CCLK (Counter Clock)
   QH  7│           │10  CCLR (Counter Clear, active LOW)
  GND  8│           │9   QH' (Serial Out)
        └───────────┘
```

## 問題分析

### BCM 21 が常に 0 の原因候補

1. **Pin 1 (QB) が実際には GND に接続されている**
   - 配線ミス
   - または pin 1 が物理的に破損

2. **Pin 1 が実は QB ではない（ICが別型番）**
   - 74HC590 ではなく別のカウンタIC
   - または非標準のピン配置

3. **QB 出力が壊れている**
   - IC の物理的故障

### 確認方法

#### 1. 物理配線チェック
```
ラズパイ物理40p (BCM 21) → 74HC590 の pin 1 に接続されているか確認
```

#### 2. マルチメータで電圧測定
```
74HC590 の pin 1 を直接測定:
- 0V → GND に短絡している（配線ミス）
- 3.3V または変動 → 正常にドライブされている
- フロート → 未接続
```

#### 3. IC の型番確認
```
ICに印字されている型番を確認:
- TC74HC590AP (東芝)
- 74HC590 (他メーカー)
- または別の型番？
```

## 次のステップ

### 即座に確認すべきこと

1. **ラズパイの物理40p がどこに接続されているか目視確認**

2. **74HC590 の pin 1 の電圧測定**
   ```
   マルチメータで測定:
   - DC電圧モード
   - 黒プローブ: GND (pin 8)
   - 赤プローブ: pin 1
   ```

3. **IC の型番を再確認**
   - IC本体に印字されている文字列を全て報告してください

### 仮説検証

もし pin 1 が GND に接続されている場合:
```
D0 = 1 (QA = 1)
D1 = 0 (QB = 0) ← GND接続のため
D2 = 1 (QC = 1)
→ 101 = 5 になる説明がつく
```

実際のカウンタ値は **0x05 ではなく、別の値（例: 0x03 = 011）が QB=0 で隠されている可能性**

## 推奨アクション

1. **物理40p の接続を外して再度測定**
   ```
   sudo python3 read_all_74hc590_pins.py
   ```
   → BCM 21 が 1 になるか 0 のままか確認

2. **別のGPIOピンで pin 1 を読む**
   - 例: BCM 22 (物理15p) を使って pin 1 を読んでみる

3. **テストコード実行**
   - 作成します
